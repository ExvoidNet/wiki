/* 
    WARNING, WORK IN PROGRESS - NOT READY FOR PRODUCTION
    * Written by a total TypeScript newbie,
    * Inspired by Docusaurus' own Team showcase,
    * In our case we are using simple HTML Tables, this code won't work appropriately with them.
*/

/*
import React, {ReactNode} from 'react';

interface ProfileProps {
    className?: string;
    name: string;
    children: ReactNode;
    skinUrl?: string;
}

function StaffShowcase({
    className,
    name,
    children,
    skinUrl,
}: ProfileProps) {
    return (
        <div className={className}>
            <div className="skin">
                <div className="rank">
                    <img 
                      className="skin__link"
                      src={`${skinUrl}.png`}
                      alt={`${name} Skin`}
                    />
                </div>
            </div>
        </div>
    );
}

export function Staff(): JSX.Element {
    return (
    )
}
*/